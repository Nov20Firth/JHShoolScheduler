# 更新日誌

## v0.1.0 — 2026-07-31

### 已知問題
- 綁課目前只能使用"固定排課"來實現
  - 因此綁課的同時必須填寫綁課時間
    - 舉例：
      > "701本土與"綁"702本土語"，"綁於：週一3"
      => 701週一3固定排課本土語 + 702週一3固定排課本土語，

### 新增

- 新增舊版 `input.txt` 匯入功能。
  - 支援 `S`、`C`、`F`、`SP`、`U` 資料。
  - 缺少任一類資料時仍可正常匯入。
  - 格式損壞時只簡單提示錯誤行號。
  - 成功匯入時不顯示彈出訊息。
  - 檔案只在瀏覽器本機讀取，不會上傳。
- 新增「指定時段同步綁課」。
  - 支援一般綁班與特教抽離。
  - 一般綁班輸出相同時段的多筆 `F`。
  - 特教抽離繼續輸出 `F` 與 `SP`。
  - 綁課類型使用單選按鈕，兩種類型只能擇一。
- 新增主任、組長快速選取按鈕。
  - 全選主任。
  - 全選組長。
- 新增資料保存提醒：重新整理（F5）後，已填內容會全部消失。
- 移除教師前新增簡單確認，並建議資料錯誤時清除重填。

### 綁課規則

- 一般綁班依「不同科目」計算綁課時段：
  - 數學 4 堂＋數學 4 堂，產生 4 個時段。
  - 國文 5 堂＋數學 4 堂，產生 9 個時段。
- 產生一般綁課時段後，各班科目由使用者逐格選擇。
- 時段中的科目選單只顯示本次綁課選過的科目。
- 綁課科目按每週堂數由多至少排序；堂數相同時按科目名稱排序。

### 介面改善

- 將「同步綁課」移到「必排條件」前面。
- 將步驟 4 改為「綁課與必排」。
- 簡化說明與按鈕文字：
  - 「產生綁課時段」。
  - 「加入必排」。
  - 「加入不可排」。
- 有既有課程資料時變更班級數，改為簡單確認，不再直接禁止修改。
- 學校資料旁新增簡短提示：學校名稱不可包含 `|`。

### 驗證與資料處理

- 保留每班 35 節檢查。
- 保留課程輸入格式及正整數堂數檢查。
- 保留必排條件重複檢查。
- 保留不可排時段自動去重。
- 保留學校名稱、學年度及 `input.txt` 分隔符號檢查。
- 不新增課程重複及錯誤班級的即時防呆，避免程式過度複雜。

### 發布

- 將 `step1courses.html` 更名為 `index.html`，舊檔名不保留。
- 更新並推送至 GitHub：
  - 儲存庫：[Nov20Firth/JHShoolScheduler](https://github.com/Nov20Firth/JHShoolScheduler)
  - 分支：`main`
  - Commit：[`5c9e5df`](https://github.com/Nov20Firth/JHShoolScheduler/commit/5c9e5df)
- 推送前完成 JavaScript 語法檢查。
